name: mobx_covid19
description: A flutter project to receive information for covida 19 in brazil and world.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^3.0.9
  flutter_modular: ^3.0.2
  flutter_mobx: ^1.1.0+2
  mobx: ^1.2.1+4
  cupertino_icons: ^0.1.3
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  mobx_codegen: ^1.0.2
  build_runner: ^1.8.0

flutter:

  
  uses-material-design: true

  assets:
   - assets/
  
